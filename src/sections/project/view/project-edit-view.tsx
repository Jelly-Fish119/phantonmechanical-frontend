import { Card, CardContent, Typography } from '@mui/material';

export function ProjectEditView() {
  return (
    <Card>
      <CardContent>
        <Typography variant="h6">Project Edit</Typography>
      </CardContent>
    </Card>
  );
}
